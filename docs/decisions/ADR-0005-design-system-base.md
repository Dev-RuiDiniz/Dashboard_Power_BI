# ADR-0005: Design system base

## Decisão

Manter um design system base dentro do app web enquanto a biblioteca compartilhada ainda não existe.

## Motivo

O repositório já possui componentes reutilizáveis em `apps/web/src/components/ui` e uma rota de demonstração em `/design-system`.
